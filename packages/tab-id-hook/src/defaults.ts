/**
 * The default query parameter key used to store the tab ID.
 */
export const SEARCH_PARAM_KEY = "tid";

/**
 * The default session storage key used to persist the tab ID.
 */
export const SESSION_STORAGE_KEY = "tab-id";
